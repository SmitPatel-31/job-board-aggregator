name: Deploy static content to Pages

on:
  # Disabled: GitHub Pages requires a paid plan on private repositories, so
  # these triggers would fail on every run. The site is served by nginx on the
  # self-hosted box instead (deploy/nginx-jobboard.conf).
  # Re-enable if the repo is made public.
  #
  # schedule:
  #   - cron: "0 0 * * *"
  # workflow_run:
  #   workflows: ["Update Job Listings"]
  #   types: [completed]
  #   branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "."
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
